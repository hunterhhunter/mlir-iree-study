챕터 2는 TableGen의 사용법을 알려주어 사용자 정의 언어가 AST수준(Ch1)이 아닌 IR 수준으로 표현되는 것을 경험할 수 있도록 지원하는 챕터이다.

MLIR은 TableGen이라는 컴파일러 코드 생성기를 지원하여 고유의 문법을 가진 .td 파일을 mlir-tblgen 명령어로 컴파일시 컴파일러 코드가 생성된다.

.toy 파일이 파싱되어 AST로 변환되고, AST 코드가 실제 MLIR로 변환되는 코드는 MLIRGen.cpp 파일에 구현이 되어있어 보면서 공부하니 훨씬 이해가 잘 되었음.